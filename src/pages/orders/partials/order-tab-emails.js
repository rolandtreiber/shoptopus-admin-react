import {Fragment} from "react";

const OrderTabEmails = ({data}) => {
  return (
    <Fragment>
      <h1>Emails</h1>
      {/*<div>{JSON.stringify(data)}</div>*/}
    </Fragment>
  )
}

export default OrderTabEmails