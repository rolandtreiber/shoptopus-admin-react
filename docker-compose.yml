version: '3.7'

services:

  admin:
    container_name: admin
    build:
      context: ./dockerfiles
      dockerfile: npm.dockerfile
    volumes:
      - ./:/usr/src/app
    ports:
      - 3000:3000
    stdin_open: true
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: npm start